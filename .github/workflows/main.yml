name: Create Release

on: push

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Generate release tag
        uses: mosfetlang/dev-tagger-action@v1
        with:
          tag-prefix: 'v'
          origin: 'origin'
